// examples/demo.ltl — runnable demo for ltl_h3_stub

import std::io
use ltl_h3_stub as lib

fn main() {
    io::println("ltl_h3_stub demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}